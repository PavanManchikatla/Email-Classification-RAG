# Copy this file to .env and fill in your values.
# NEVER commit the actual .env file.

# Required: Anthropic API key for generating training labels
ANTHROPIC_API_KEY=your-api-key-here

# Optional: Gmail config overrides
# GMAIL_CREDENTIALS_FILE=credentials.json
# GMAIL_TOKEN_FILE=token.json
# GMAIL_MAX_RESULTS_PER_PAGE=100
# GMAIL_MAX_PAGES=5

# Optional: Bulk ingestion settings (for multi-account fetch)
# GMAIL_BULK_MAX_PAGES=200
# GMAIL_TARGET_TOTAL_EMAILS=15000

# Optional: LLM model for label generation
# LLM_MODEL=claude-haiku-4-5-20251001

# Optional: Classification batch size
# CLASSIFY_BATCH_SIZE=10

# Optional: Logging level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO
